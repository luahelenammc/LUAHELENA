import fs from 'node:fs';import path from 'node:path';const sourceRoot=path.resolve(new URL('..',import.meta.url).pathname);const siteRoot=path.resolve(sourceRoot,'..','moonverse');const read=(p)=>fs.readFileSync(path.join(siteRoot,p),'utf8');const json=(p)=>JSON.parse(fs.readFileSync(path.join(sourceRoot,'v4',p),'utf8'));const pages=json('public-pages.json').pages,rooms=json('rooms.json').rooms,albums=json('albums.json').albums;const checks=[];const assert=(ok,msg)=>{if(!ok)throw new Error(msg);checks.push(msg)};const home=read('index.html'),wiki=read('wiki/index.html'),timeline=read('timeline/index.html'),atlas=read('atlas/index.html'),albumsHtml=read('albums/index.html'),memory=read('memory/index.html'),js=read('assets/v4.js'),css=read('assets/v4.css');assert(home.includes('<section class="hall"'),'home is a Hall scene');assert(home.includes('/moonverse/room/biblioteca-lunar/'),'Hall opens Biblioteca');assert(home.includes('/moonverse/room/quarto-nostalgico/'),'Hall opens Quarto Nostálgico');assert(home.includes('/moonverse/room/observatorio-nexus/'),'Hall opens Observatório');assert(home.includes('/moonverse/memory/'),'Hall exposes the Mnemonic Machine');assert(home.includes('/moonverse/albums/'),'Hall exposes albums');assert(!home.includes('Uma entrada para começar'),'V3 editorial-home feature block is gone');assert(!home.includes('Leitura primeiro'),'V3 reading-first hierarchy is gone');for(const room of rooms){const html=read(`room/${room.id}/index.html`);assert(html.includes('room-stage'),`${room.id} renders as scene`);assert(html.includes('<h1>'),`${room.id} has heading`);assert(html.includes('room-object'),`${room.id} has interactive or locked objects`)}for(const page of pages){assert(wiki.includes(page.url),`${page.id} remains reachable from Moonpedia`);const rel=new URL(page.url,'https://moonverse.local').pathname.replace(/^\/moonverse\//,'');const article=read(`${rel}index.html`);assert(article.includes('<article class="article">'),`${page.id} retains V3 deep-reading article primitive`)}assert(timeline.includes('2004')&&timeline.includes('2025'),'timeline uses biographical source dates');assert(!timeline.includes('2026-08-02'),'timeline is not sorted from article publication dates');assert(atlas.includes('Lista completa'),'Atlas has textual fallback');assert(atlas.includes('data-atlas-filter'),'Atlas has projection filters');assert(albums.filter(a=>a.status==='populated').length>=2,'at least two albums are populated');assert(albumsHtml.includes('data-album-toggle'),'albums have interactive covers');assert(memory.includes('data-memory-pick'),'Mnemonic Machine is operational');assert(memory.includes('Ver todas as memórias sem sorteio'),'memory mode has no-JS fallback');assert(js.includes('prefers-color-scheme: dark'),'theme respects OS preference on first visit');assert(js.includes("fetch('/moonverse/assets/search-index.json')"),'search reuses approved V3 public index');assert(css.includes('prefers-reduced-motion'),'V4 respects reduced motion');console.log(`Moonverse V4 smoke tests passed: ${checks.length} checks, ${rooms.length} rooms, ${pages.length} public documents.`);
